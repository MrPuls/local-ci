# Dev connection to a running `local-ci serve`. Copy to web/.env and edit.
# Start the server with matching flags, e.g.:
#   local-ci serve --port 4123 --token dev
VITE_LCI_PORT=4123
VITE_LCI_TOKEN=dev
